name: Production Build & IPA Release

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 1. Setup Directories and Process Logos
        run: |
          mkdir -p PokeApp/Assets.xcassets/AppIcon.appiconset
          mkdir -p PokeApp/Assets.xcassets/LaunchLogo.imageset
          mkdir -p PokeApp/Assets.xcassets/LaunchBackground.colorset

          if [ -f "poke.png" ]; then
            sips -Z 1024 "poke.png" --out "PokeApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png"
            echo "Successfully processed 'poke.png' into AppIcon"
          fi

          if [ -f "splash_screen.png" ]; then
            sips -Z 360 "splash_screen.png" --out "PokeApp/Assets.xcassets/LaunchLogo.imageset/LaunchLogo.png"
            echo "Successfully processed 'splash_screen.png' into LaunchLogo"
          fi

      - name: 2. Generate project.yml
        run: |
          cat << 'EOF' > project.yml
          name: Poke
          options:
            bundleIdPrefix: com.poke
            deploymentTarget:
              iOS: "15.0"
            xcodeVersion: "15.0"
          targets:
            Poke:
              type: application
              platform: iOS
              sources: [PokeApp]
              settings:
                PRODUCT_NAME: "Poke"
                PRODUCT_BUNDLE_IDENTIFIER: com.poke.app
                INFOPLIST_FILE: PokeApp/Info.plist
                DEVELOPMENT_TEAM: ""
                CODE_SIGNING_REQUIRED: "NO"
                CODE_SIGN_IDENTITY: ""
                CODE_SIGNING_ENTITLEMENTS: ""
                CODE_SIGNING_ALLOWED: "NO"
          EOF

      - name: 3. Generate Info.plist with Auto-Incremented Version
        run: |
          cat << EOF > PokeApp/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>\$(EXECUTABLE_NAME)</string>
              <key>CFBundleIdentifier</key>
              <string>\$(PRODUCT_BUNDLE_IDENTIFIER)</string>
              <key>CFBundleDisplayName</key>
              <string>Poke</string>
              <key>CFBundleName</key>
              <string>Poke</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0.${{ github.run_number }}</string>
              <key>CFBundleVersion</key>
              <string>${{ github.run_number }}</string>
              <key>GitHubRepository</key>
              <string>${{ github.repository }}</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>UIApplicationSceneManifest</key>
              <dict>
                  <key>UIApplicationSupportsMultipleScenes</key>
                  <false/>
                  <key>UISceneConfigurations</key>
                  <dict>
                      <key>UIWindowSceneSessionRoleApplication</key>
                      <array>
                          <dict>
                              <key>UISceneConfigurationName</key>
                              <string>Default Configuration</string>
                              <key>UISceneDelegateClassName</key>
                              <string>\$(PRODUCT_MODULE_NAME).SceneDelegate</string>
                          </dict>
                      </array>
                  </dict>
              </dict>
              <key>UILaunchStoryboardName</key>
              <string>LaunchScreen</string>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
              </array>
              <key>NSAppTransportSecurity</key>
              <dict>
                  <key>NSAllowsArbitraryLoads</key>
                  <true/>
              </dict>
          </dict>
          </plist>
          EOF

      - name: 4. Generate LaunchScreen.storyboard
        run: |
          cat << 'EOF' > PokeApp/LaunchScreen.storyboard
          <?xml version="1.0" encoding="UTF-8"?>
          <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB" version="3.0" toolsVersion="21701" targetRuntime="iOS.CocoaTouch" propertyAccessControl="none" useAutolayout="YES" launchScreen="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="01J-lp-oVM">
              <device id="retina6_12" orientation="portrait" appearance="light"/>
              <dependencies>
                  <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="21679"/>
                  <capability name="Safe area layout guides" minToolsVersion="9.0"/>
                  <capability name="documents saved in the Xcode 8 format" minToolsVersion="8.0"/>
              </dependencies>
              <scenes>
                  <scene sceneID="EHf-IW-A2E">
                      <objects>
                          <viewController id="01J-lp-oVM" sceneMemberID="viewController">
                              <view key="view" contentMode="scaleToFill" id="Ze5-6b-2t3">
                                  <rect key="frame" x="0.0" y="0.0" width="393" height="852"/>
                                  <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
                                  <subviews>
                                      <imageView clipsSubviews="YES" userInteractionEnabled="NO" contentMode="scaleAspectFit" horizontalHuggingPriority="251" verticalHuggingPriority="251" image="LaunchLogo" translatesAutoresizingMaskIntoConstraints="NO" id="Y6W-OH-hqX">
                                          <rect key="frame" x="106.5" y="336" width="180" height="180"/>
                                          <constraints>
                                              <constraint firstAttribute="width" constant="180" id="w-180"/>
                                              <constraint firstAttribute="height" constant="180" id="h-180"/>
                                          </constraints>
                                      </imageView>
                                  </subviews>
                                  <viewLayoutGuide key="safeArea" id="6Tk-OE-BBY"/>
                                  <color key="backgroundColor" white="0.0" alpha="1" colorSpace="custom" customColorSpace="genericGamma22GrayColorSpace"/>
                                  <constraints>
                                      <constraint firstItem="Y6W-OH-hqX" firstAttribute="centerX" secondItem="Ze5-6b-2t3" secondAttribute="centerX" id="cx-center"/>
                                      <constraint firstItem="Y6W-OH-hqX" firstAttribute="centerY" secondItem="Ze5-6b-2t3" secondAttribute="centerY" id="cy-center"/>
                                  </constraints>
                              </view>
                          </viewController>
                          <placeholder placeholderIdentifier="IBFirstResponder" id="iYj-Kq-Ea1" userLabel="First Responder" sceneMemberID="firstResponder"/>
                      </objects>
                      <point key="canvasLocation" y="53" x="375"/>
                  </scene>
              </scenes>
              <resources>
                  <image name="LaunchLogo" width="180" height="180"/>
              </resources>
          </document>
          EOF

      - name: 5. Generate App Delegates
        run: |
          cat << 'EOF' > PokeApp/AppDelegate.swift
          import UIKit

          @main
          class AppDelegate: UIResponder, UIApplicationDelegate {
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  return true
              }

              func application(_ application: UIApplication, configurationForConnecting connectingSceneSession: UISceneSession, options: UIScene.ConnectionOptions) -> UISceneConfiguration {
                  return UISceneConfiguration(name: "Default Configuration", sessionRole: connectingSceneSession.role)
              }
          }
          EOF

          cat << 'EOF' > PokeApp/SceneDelegate.swift
          import UIKit

          class SceneDelegate: UIResponder, UIWindowSceneDelegate {
              var window: UIWindow?

              func scene(_ scene: UIScene, willConnectTo session: UISceneSession, options connectionOptions: UIScene.ConnectionOptions) {
                  guard let windowScene = (scene as? UIWindowScene) else { return }
                  
                  let window = UIWindow(windowScene: windowScene)
                  let viewController = ViewController()
                  window.rootViewController = viewController
                  self.window = window
                  window.makeKeyAndVisible()
              }
          }
          EOF

      - name: 6. Generate ViewController.swift
        run: |
          cat << 'EOF' > PokeApp/ViewController.swift
          import UIKit
          import WebKit

          class CustomWebView: WKWebView {
              override var inputAccessoryView: UIView? {
                  return nil
              }

              override func canPerformAction(_ action: Selector, withSender sender: Any?) -> Bool {
                  return false
              }
          }

          class ViewController: UIViewController, WKScriptMessageHandler, WKNavigationDelegate, WKUIDelegate, UIScrollViewDelegate {
              
              private var webView: CustomWebView!
              private var splashOverlay: UIView!
              private var isSplashFaded = false
              
              private let lightHaptic = UIImpactFeedbackGenerator(style: .light)
              private let mediumHaptic = UIImpactFeedbackGenerator(style: .medium)
              private let heavyHaptic = UIImpactFeedbackGenerator(style: .heavy)
              private let selectionHaptic = UISelectionFeedbackGenerator()
              private let notificationHaptic = UINotificationFeedbackGenerator()

              override func viewDidLoad() {
                  super.viewDidLoad()
                  view.backgroundColor = .black
                  prepareHaptics()
                  setupWebView()
                  setupSplashOverlay()
                  loadTargetURL()
                  checkForUpdates()
              }

              private func prepareHaptics() {
                  lightHaptic.prepare()
                  mediumHaptic.prepare()
                  heavyHaptic.prepare()
                  selectionHaptic.prepare()
                  notificationHaptic.prepare()
              }

              private func setupSplashOverlay() {
                  splashOverlay = UIView(frame: view.bounds)
                  splashOverlay.backgroundColor = .black
                  splashOverlay.autoresizingMask = [.flexibleWidth, .flexibleHeight]
                  
                  if let logoImage = UIImage(named: "LaunchLogo") {
                      let imageView = UIImageView(image: logoImage)
                      imageView.contentMode = .scaleAspectFit
                      imageView.translatesAutoresizingMaskIntoConstraints = false
                      splashOverlay.addSubview(imageView)
                      
                      NSLayoutConstraint.activate([
                          imageView.centerXAnchor.constraint(equalTo: splashOverlay.centerXAnchor),
                          imageView.centerYAnchor.constraint(equalTo: splashOverlay.centerYAnchor),
                          imageView.widthAnchor.constraint(equalToConstant: 180),
                          imageView.heightAnchor.constraint(equalToConstant: 180)
                      ])
                  }
                  
                  view.addSubview(splashOverlay)
                  
                  DispatchQueue.main.asyncAfter(deadline: .now() + 3.0) { [weak self] in
                      self?.dismissSplashOverlay()
                  }
              }

              private func dismissSplashOverlay() {
                  guard !isSplashFaded else { return }
                  isSplashFaded = true
                  
                  UIView.animate(withDuration: 0.5, delay: 0.1, options: .curveEaseInOut, animations: {
                      self.splashOverlay.alpha = 0.0
                  }) { _ in
                      self.splashOverlay.removeFromSuperview()
                  }
              }

              private func setupWebView() {
                  let contentController = WKUserContentController()
                  
                  contentController.add(self, name: "nativeHaptic")
                  contentController.add(self, name: "buttonPressedHandler")
                  
                  let jsScript = """
                  (function() {
                      var meta = document.querySelector('meta[name="viewport"]');
                      if (!meta) {
                          meta = document.createElement('meta');
                          meta.name = 'viewport';
                          document.head.appendChild(meta);
                      }
                      meta.content = 'width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover';

                      window.PokeNative = {
                          haptic: function(style) {
                              if (window.webkit && window.webkit.messageHandlers && window.webkit.messageHandlers.nativeHaptic) {
                                  window.webkit.messageHandlers.nativeHaptic.postMessage(style || 'medium');
                              }
                          }
                      };

                      var style = document.createElement('style');
                      style.id = 'poke-strict-native-styles';
                      style.innerHTML = `
                          *, *::before, *::after {
                              -webkit-touch-callout: none !important;
                              -webkit-user-select: none !important;
                              -khtml-user-select: none !important;
                              -moz-user-select: none !important;
                              -ms-user-select: none !important;
                              user-select: none !important;
                              -webkit-tap-highlight-color: transparent !important;
                              -webkit-user-drag: none !important;
                          }
                          input, textarea, [contenteditable="true"] {
                              -webkit-user-select: text !important;
                              user-select: text !important;
                              caret-color: #007aff !important;
                              font-size: 16px !important;
                          }
                          ::-webkit-scrollbar {
                              display: none !important;
                          }

                          [data-silk-sheet-wrapper] {
                              margin-top: max(env(safe-area-inset-top, 16px), 16px) !important;
                              border-top-left-radius: 20px !important;
                              border-top-right-radius: 20px !important;
                          }
                      `;
                      document.head.appendChild(style);

                      document.addEventListener('selectstart', function(e) {
                          var tag = e.target.tagName ? e.target.tagName.toUpperCase() : '';
                          if (tag !== 'INPUT' && tag !== 'TEXTAREA') {
                              e.preventDefault();
                          }
                      }, true);
                      
                      document.addEventListener('contextmenu', function(e) { e.preventDefault(); }, true);

                      function applyOption2Layout() {
                          ['Recipes', 'Message'].forEach(function(name) {
                              var el = document.querySelector('[aria-label="' + name + '"]');
                              if (!el) {
                                  var elements = Array.from(document.querySelectorAll('button, a'));
                                  el = elements.find(function(item) {
                                      return item.innerText && item.innerText.trim() === name;
                                  });
                              }
                              if (el) {
                                  var wrapper = el.closest('.min-w-0') || el;
                                  wrapper.remove();
                              }
                          });

                          var mailBtn = document.querySelector('[aria-label="Mail"]') ||
                                         Array.from(document.querySelectorAll('a')).find(function(el) {
                                             return el.innerText && el.innerText.trim() === 'Mail';
                                         });
                          
                          if (mailBtn) {
                              var mailWrapper = mailBtn.closest('.min-w-0');
                              if (mailWrapper) {
                                  mailWrapper.style.width = '100%';
                                  mailWrapper.style.flex = '1 1 100%';
                              }
                          }

                          injectKreatixSection();
                      }

                      function injectKreatixSection() {
                          if (document.getElementById('kreatix-custom-row')) return;

                          var twitterEl = Array.from(document.querySelectorAll('a, button, div')).find(function(el) {
                              return el.innerText && el.innerText.trim() === 'Twitter';
                          });

                          if (!twitterEl) return;

                          var twitterLink = twitterEl.tagName === 'A' ? twitterEl : twitterEl.closest('a');
                          if (!twitterLink || !twitterLink.parentNode) return;

                          twitterLink.style.borderBottomLeftRadius = '0px';
                          twitterLink.style.borderBottomRightRadius = '0px';

                          var row = document.createElement('a');
                          row.id = 'kreatix-custom-row';
                          row.href = 'https://x.com/KreatixDev';
                          row.target = '_blank';
                          row.rel = 'noopener noreferrer';
                          row.className = twitterLink.className || '';
                          
                          row.style.cssText = `
                              display: flex !important;
                              flex-direction: row !important;
                              align-items: center !important;
                              justify-content: space-between !important;
                              width: 100% !important;
                              box-sizing: border-box !important;
                              padding: 14px 16px !important;
                              border-top: 1px solid rgba(0, 0, 0, 0.06) !important;
                              border-bottom-left-radius: 20px !important;
                              border-bottom-right-radius: 20px !important;
                              text-decoration: none !important;
                          `;

                          row.innerHTML = `
                              <div style="display: flex !important; align-items: center !important; gap: 12px !important; min-width: 0 !important; flex: 1 !important;">
                                  <svg style="width: 18px !important; height: 18px !important; min-width: 18px !important; fill: #4b5563 !important; flex-shrink: 0 !important;" viewBox="0 0 24 24">
                                      <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                                  </svg>
                                  <span style="font-size: 14px !important; font-weight: 500 !important; color: #1f2937 !important; white-space: nowrap !important; overflow: hidden !important; text-overflow: ellipsis !important;">iOS app made by KreatixDev</span>
                              </div>
                              <svg style="width: 16px !important; height: 16px !important; min-width: 16px !important; stroke: #9ca3af !important; fill: none !important; flex-shrink: 0 !important; margin-left: 8px !important;" viewBox="0 0 24 24">
                                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                              </svg>
                          `;

                          twitterLink.parentNode.insertBefore(row, twitterLink.nextSibling);

                          var cardContainer = twitterLink;
                          while (cardContainer && cardContainer.parentElement) {
                              if (cardContainer.classList && (cardContainer.classList.contains('rounded-3xl') || cardContainer.classList.contains('rounded-2xl') || cardContainer.classList.contains('border'))) {
                                  break;
                              }
                              cardContainer = cardContainer.parentElement;
                          }

                          if (cardContainer && cardContainer.parentNode && !document.getElementById('kreatix-disclaimer')) {
                              var disclaimer = document.createElement('div');
                              disclaimer.id = 'kreatix-disclaimer';
                              disclaimer.style.cssText = 'width: 100% !important; text-align: center !important; font-size: 11px !important; font-weight: 500 !important; color: #9ca3af !important; margin-top: 12px !important; margin-bottom: 8px !important; opacity: 0.8 !important; display: block !important; clear: both !important;';
                              disclaimer.innerText = 'Not affiliated with Interaction or Cognition';
                              cardContainer.parentNode.insertBefore(disclaimer, cardContainer.nextSibling);
                          }
                      }

                      applyOption2Layout();
                      var observer = new MutationObserver(applyOption2Layout);
                      observer.observe(document.body || document.documentElement, { childList: true, subtree: true });

                      var lastTouchTime = 0;
                      function handleTouchStart(e) {
                          var target = e.target;
                          if (!target) return;

                          var btn = target.closest('button, a[href], [role="button"], [aria-label="About"], [aria-label="Settings"], [aria-label="Automations"], [aria-label="Integrations"], [aria-label="Mail"]');
                          
                          if (!btn) return;

                          var rect = btn.getBoundingClientRect();
                          if (rect.width > window.innerWidth * 0.95 && rect.height > window.innerHeight * 0.90) return;

                          var now = Date.now();
                          if (now - lastTouchTime < 120) return;
                          lastTouchTime = now;

                          var label = btn.getAttribute ? btn.getAttribute('aria-label') : '';
                          if (label === 'About' || label === 'Settings' || (btn.querySelector && btn.querySelector('img[alt="Avatar"]'))) {
                              window.PokeNative.haptic('selection');
                          } else {
                              window.PokeNative.haptic('medium');
                          }
                      }

                      document.addEventListener('touchstart', handleTouchStart, { capture: true, passive: true });
                      document.addEventListener('mousedown', handleTouchStart, { capture: true, passive: true });
                  })();
                  """
                  
                  let userScript = WKUserScript(source: jsScript, injectionTime: .atDocumentEnd, forMainFrameOnly: false)
                  contentController.addUserScript(userScript)

                  let config = WKWebViewConfiguration()
                  config.userContentController = contentController
                  config.allowsInlineMediaPlayback = true

                  webView = CustomWebView(frame: .zero, configuration: config)
                  webView.navigationDelegate = self
                  webView.uiDelegate = self
                  
                  webView.scrollView.delegate = self
                  webView.scrollView.minimumZoomScale = 1.0
                  webView.scrollView.maximumZoomScale = 1.0
                  webView.scrollView.bounces = false
                  webView.scrollView.contentInsetAdjustmentBehavior = .never
                  webView.isOpaque = false
                  webView.backgroundColor = .black

                  webView.translatesAutoresizingMaskIntoConstraints = false
                  view.addSubview(webView)

                  NSLayoutConstraint.activate([
                      webView.topAnchor.constraint(equalTo: view.topAnchor),
                      webView.bottomAnchor.constraint(equalTo: view.bottomAnchor),
                      webView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
                      webView.trailingAnchor.constraint(equalTo: view.trailingAnchor)
                  ])
              }

              private func loadTargetURL() {
                  if let url = URL(string: "https://poke.com/home") {
                      let request = URLRequest(url: url)
                      webView.load(request)
                  }
              }

              private func checkForUpdates() {
                  guard let repo = Bundle.main.object(forInfoDictionaryKey: "GitHubRepository") as? String,
                        !repo.isEmpty, !repo.contains("$") else { return }
                  
                  let apiUrlString = "https://api.github.com/repos/\(repo)/releases/latest"
                  guard let apiUrl = URL(string: apiUrlString) else { return }
                  
                  var request = URLRequest(url: apiUrl)
                  request.addValue("application/json", forHTTPHeaderField: "Accept")
                  request.timeoutInterval = 10.0
                  
                  URLSession.shared.dataTask(with: request) { [weak self] data, response, error in
                      guard let data = data, error == nil else { return }
                      
                      guard let json = try? JSONSerialization.jsonObject(with: data) as? [String: Any],
                            let tagName = json["tag_name"] as? String else { return }
                      
                      let currentVersion = Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "1.0.0"
                      let latestVersion = tagName.trimmingCharacters(in: CharacterSet(charactersIn: "vV"))
                      let currentClean = currentVersion.trimmingCharacters(in: CharacterSet(charactersIn: "vV"))
                      
                      if latestVersion != currentClean {
                          DispatchQueue.main.async {
                              self?.showUpdateAlert()
                          }
                      }
                  }.resume()
              }

              private func showUpdateAlert() {
                  let alert = UIAlertController(
                      title: "Update Available",
                      message: nil,
                      preferredStyle: .alert
                  )
                  
                  alert.addAction(UIAlertAction(title: "Update", style: .default) { _ in
                      if let downloadUrl = URL(string: "https://github.com/XFlexDev/poke-app/releases/latest/download/Poke.ipa") {
                          UIApplication.shared.open(downloadUrl, options: [:], completionHandler: nil)
                      }
                  })
                  
                  alert.addAction(UIAlertAction(title: "Later", style: .cancel, handler: nil))
                  
                  present(alert, animated: true, completion: nil)
              }

              private func isAllowedInApp(url: URL) -> Bool {
                  guard let host = url.host?.lowercased() else { return true }
                  
                  guard host == "poke.com" || host == "www.poke.com" else {
                      return false
                  }
                  
                  let path = url.path.lowercased()
                  
                  if path == "" || path == "/" ||
                     path == "/home" || path == "/home/" ||
                     path == "/about" || path == "/about/" ||
                     path.hasPrefix("/settings") ||
                     path.hasPrefix("/automations") ||
                     path.hasPrefix("/inbox") ||
                     path.hasPrefix("/integrations") {
                      return true
                  }
                  
                  return false
              }

              func viewForZooming(in scrollView: UIScrollView) -> UIView? {
                  return nil
              }

              // MARK: - WKNavigationDelegate
              func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
                  guard let url = navigationAction.request.url else {
                      decisionHandler(.allow)
                      return
                  }
                  
                  if navigationAction.targetFrame?.isMainFrame == true {
                      if isAllowedInApp(url: url) {
                          decisionHandler(.allow)
                      } else {
                          UIApplication.shared.open(url, options: [:], completionHandler: nil)
                          decisionHandler(.cancel)
                      }
                  } else {
                      decisionHandler(.allow)
                  }
              }

              func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
                  dismissSplashOverlay()
              }

              func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
                  dismissSplashOverlay()
              }

              func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
                  dismissSplashOverlay()
              }

              // MARK: - WKUIDelegate
              func webView(_ webView: WKWebView, createWebViewWith configuration: WKWebViewConfiguration, for navigationAction: WKNavigationAction, windowFeatures: WKWindowFeatures) -> WKWebView? {
                  if let url = navigationAction.request.url {
                      if isAllowedInApp(url: url) {
                          webView.load(navigationAction.request)
                      } else {
                          UIApplication.shared.open(url, options: [:], completionHandler: nil)
                      }
                  }
                  return nil
              }

              // MARK: - WKScriptMessageHandler
              func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
                  if message.name == "nativeHaptic" {
                      if let type = message.body as? String {
                          switch type {
                          case "light":
                              lightHaptic.impactOccurred()
                          case "medium":
                              mediumHaptic.impactOccurred()
                          case "heavy":
                              heavyHaptic.impactOccurred()
                          case "selection":
                              selectionHaptic.selectionChanged()
                          case "success":
                              notificationHaptic.notificationOccurred(.success)
                          case "warning":
                              notificationHaptic.notificationOccurred(.warning)
                          case "error":
                              notificationHaptic.notificationOccurred(.error)
                          default:
                              mediumHaptic.impactOccurred()
                          }
                      }
                  }
              }
          }
          EOF

      - name: 7. Generate Asset Catalogs
        run: |
          cat << 'EOF' > PokeApp/Assets.xcassets/Contents.json
          {
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          EOF

          cat << 'EOF' > PokeApp/Assets.xcassets/AppIcon.appiconset/Contents.json
          {
            "images" : [
              {
                "filename" : "AppIcon-1024.png",
                "idiom" : "universal",
                "platform" : "ios",
                "size" : "1024x1024"
              }
            ],
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          EOF

          cat << 'EOF' > PokeApp/Assets.xcassets/LaunchBackground.colorset/Contents.json
          {
            "colors" : [
              {
                "color" : {
                  "color-space" : "srgb",
                  "components" : {
                    "alpha" : "1.000",
                    "blue" : "0.000",
                    "green" : "0.000",
                    "red" : "0.000"
                  }
                },
                "idiom" : "universal"
              }
            ],
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          EOF

          cat << 'EOF' > PokeApp/Assets.xcassets/LaunchLogo.imageset/Contents.json
          {
            "images" : [
              {
                "idiom" : "universal",
                "scale" : "1x"
              },
              {
                "filename" : "LaunchLogo.png",
                "idiom" : "universal",
                "scale" : "2x"
              },
              {
                "idiom" : "universal",
                "scale" : "3x"
              }
            ],
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          EOF

      - name: 8. Generate Professional README.md
        run: |
          cat << 'EOF' > README.md
          # Poke iOS Application

          A native iOS wrapper for `https://poke.com/home` built with Swift and WKWebView. It strips away browser chrome, URL bars, and web artifacts to feel like a native iOS app.

          ---

          ## Features

          - **Native App Feel**: Completely hides the URL bar, browser controls, and web headers.
          - **Custom Black Launch Screen**: Displays a black background with a smooth cross-fade transition on startup.
          - **Automatic Update Checker**: Queries GitHub Releases on app launch and prompts you with a direct download link whenever a new version is released.
          - **Custom Dashboard Layout**: Option 2 layout engine (`Automations` and `Integrations` side-by-side; `Mail` stretched full-width across the bottom; `Recipes` and `Message` removed).
          - **Strict Button Haptics**: Contextual tactile feedback triggered strictly when pressing actual interactive buttons, card items, or avatar links.
          - **KreatixDev Credit & Disclaimer**: Blended credit row inserted inside the About sheet pointing to `@KreatixDev` on X, accompanied by a clean disclaimer text.
          - **External Route Protection**: Navigation to allowed routes (`/home`, `/about`, `/settings/*`, `/automations/*`, `/inbox/*`, `/integrations/*`) stays in-app. All external links or secondary routes automatically open in native Safari.
          - **Clean Keyboard Integration**: Suppresses the Safari `^` `v` `Done` keyboard bar (`inputAccessoryView = nil`) while preserving the blinking typing caret (`caret-color: #007aff`).
          - **Strict Anti-Zoom & Anti-Highlight**: Blocks pinch-zoom, double-tap zoom, long-press text selection, and context menus.

          ---

          ## Installation & Sideloading Guide

          Because this is an unsigned `.ipa` build, you can sideload and sign it on your iOS device using any of the methods below.

          ### Method 1: Sideloadly (Windows / macOS) — Recommended

          1. Download and install **[Sideloadly](https://sideloadly.io/)**.
          2. Connect your iPhone to your computer via USB.
          3. Download `Poke.ipa` from the **Releases** tab of this repository.
          4. Drag and drop `Poke.ipa` into Sideloadly.
          5. Enter your **Apple ID** email.
          6. Click **Start** to sign and install the app onto your iPhone.
          7. On your iPhone, go to **Settings > General > VPN & Device Management**, tap your Apple ID, and select **Trust**.

          ---

          ### Method 2: AltStore / SideStore (On-Device Automatic Refresh)

          1. Install **[AltStore](https://altstore.io/)** or **[SideStore](https://sidestore.io/)** on your device.
          2. Download `Poke.ipa` directly onto your iPhone.
          3. Open **AltStore > My Apps**, tap the **+** button at the top left, and select `Poke.ipa`.
          4. AltStore will sign and install the app using your Apple ID.

          ---

          ### Method 3: TrollStore (iOS 14.0 – 16.6.1 / 17.0) — Permanently Signed

          If your iOS device supports **[TrollStore](https://github.com/opa334/TrollStore)**:
          1. Download `Poke.ipa` on your iOS device.
          2. Open `Poke.ipa` using TrollStore.
          3. Tap **Install**.
          4. The app will be permanently signed with no 7-day expiration limit.

          ---

          ### Method 4: LiveContainer / Scarlet / Esign

          1. Download `Poke.ipa`.
          2. Import `Poke.ipa` into **LiveContainer**, **Scarlet**, or **Esign**.
          3. Sign using your preferred mobile provision or developer certificate and install.

          ---

          ## Disclaimer

          This application is an independent client wrapper developed by KreatixDev. Not affiliated with Interaction or Cognition.
          EOF

      - name: 9. Clean Git History (Single Orphan Initial Commit)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout --orphan temp_branch
          git add .
          git commit -m "Build v1.0.${{ github.run_number }} [skip ci]"
          git branch -D main || true
          git branch -m main
          git push -f origin main

      - name: 10. Install XcodeGen
        run: brew install xcodegen

      - name: 11. Generate Xcode Project & Fix Format Compatibility
        run: |
          xcodegen generate
          sed -i '' 's/objectVersion = [0-9]*;/objectVersion = 56;/g' Poke.xcodeproj/project.pbxproj

      - name: 12. Build App for iOS (Unsigned)
        run: |
          xcodebuild -project Poke.xcodeproj \
            -scheme Poke \
            -sdk iphoneos \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: 13. Package Unsigned Standalone IPA
        run: |
          mkdir -p Payload
          cp -R build/Build/Products/Release-iphoneos/Poke.app Payload/
          zip -r Poke.ipa Payload

      - name: 14. Create GitHub Release with Direct IPA Asset
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: Poke iOS v1.0.${{ github.run_number }}
          body_path: README.md
          files: Poke.ipa
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
